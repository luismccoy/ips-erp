# ============================================================================
# SSH Configuration - Cleaned up by Kiro (2026-02-03)
# ============================================================================

### Start of WSSH Config Block ###
# WARNING: Do not edit between WSSH Config Block markers - managed by WSSH

# Off-VPN access to Cloud Desktops
Host dev-dsk-*.amazon.com
  ProxyCommand=/usr/local/bin/wssh proxy %h
  ServerAliveInterval 15
  ServerAliveCountMax 44
  User luiscoy

# Off-VPN access to corp hosts
Host *.corp.amazon.com
  ProxyCommand=/usr/local/bin/wssh proxy %h
  ServerAliveInterval 15
  ServerAliveCountMax 44
  User luiscoy

# Off-VPN Git access
Host git.amazon.com
  ProxyCommand=/usr/local/bin/wssh proxy %h --port=%p
  User luiscoy

Host github.audible.com
  ProxyCommand=/usr/local/bin/wssh proxy %h --port=%p
  User git

Match all

### End of WSSH Config Block ###


# ============================================================================
# Custom Aliases (safe to edit)
# ============================================================================

# Cloud Desktop - Primary alias (uses ECDSA - REQUIRED for OpenSSH > 8.7)
Host dev-kiro
  HostName dev-dsk-luiscoy-1e-de514647.us-east-1.amazon.com
  ProxyCommand wssh proxy %h
  User luiscoy
  IdentityFile ~/.ssh/id_ecdsa
  CertificateFile ~/.ssh/id_ecdsa-cert.pub
  ServerAliveInterval 15
  ServerAliveCountMax 44

# Ubuntu EC2 Instance (Personal AWS)
Host ubuntu-dev
  HostName 44.213.176.239
  User ubuntu
  IdentityFile ~/.ssh/id_ec2_remote_dev
  UpdateHostKeys yes
  StrictHostKeyChecking accept-new
  ForwardAgent no
  ServerAliveInterval 60
  ServerAliveCountMax 3
